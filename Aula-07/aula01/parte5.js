// Arrays

const listaDeNomes = ["Maycon", "João", "Maria", "Gugu", "Fulano"];

// length = tamanho
console.log(listaDeNomes.length); // 5

for (let indice = 0; indice < listaDeNomes.length; i++) {
  const nome = listaDeNomes[indice];
  console.log(nome);
}
